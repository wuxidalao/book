<view class='header'>
  <w-epsoide index="{{classic.index}}" />
  <w-like bind:like="onLike" like="{{classic.like_status}}" count="{{classic.fav_nums}}" />
</view>

<w-movie img="{{classic.image}}" content="{{classic.content}}" />

